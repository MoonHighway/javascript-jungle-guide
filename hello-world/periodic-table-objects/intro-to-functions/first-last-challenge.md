# First and Last Name Function Challenge

Challenge first and last name function

```javascript
const printName = (first, last) =>
  console.log(`${last}, ${first}`);

printName("Florence", "Machine");
```
